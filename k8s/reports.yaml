apiVersion: apps/v1
kind: Deployment
metadata:
  name: vetmanager-reports
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: vetmanager-reports
  template:
    metadata:
      labels:
        app: vetmanager-reports
    spec:
      serviceAccountName: vetmanager-reports
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: reports
          image: ghcr.io/ajpfox10/veterinaria-reports:81c52c17
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]
          envFrom:
            - configMapRef:
                name: vetmanager-config
            - secretRef:
                name: vetmanager-secrets
          env:
            - name: PORT
              value: "3008"
          ports:
            - containerPort: 3008
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "1000m"
              memory: "768Mi"
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3008
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3008
            initialDelaySeconds: 10
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: vetmanager-reports
spec:
  selector:
    app: vetmanager-reports
  ports:
    - port: 3008
      targetPort: 3008
