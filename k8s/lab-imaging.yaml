apiVersion: apps/v1
kind: Deployment
metadata:
  name: vetmanager-lab-imaging
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: vetmanager-lab-imaging
  template:
    metadata:
      labels:
        app: vetmanager-lab-imaging
    spec:
      serviceAccountName: vetmanager-lab-imaging
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: lab-imaging
          image: ghcr.io/ajpfox10/veterinaria-lab-imaging:81c52c17
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]
          envFrom:
            - configMapRef:
                name: vetmanager-config
            - secretRef:
                name: vetmanager-secrets
          env:
            - name: PORT
              value: "3004"
          ports:
            - containerPort: 3004
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "1000m"
              memory: "768Mi"
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3004
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3004
            initialDelaySeconds: 10
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: vetmanager-lab-imaging
spec:
  selector:
    app: vetmanager-lab-imaging
  ports:
    - port: 3004
      targetPort: 3004
