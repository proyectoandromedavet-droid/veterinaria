# Base de datos: AWS RDS — no hay contenedor MySQL local.
# Todas las credenciales MYSQL_* se leen desde .env (copiar de .env.example).

services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD} --maxmemory 512mb --maxmemory-policy allkeys-lru
    # Port NOT exposed to host — only reachable inside vetnet
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1024M
    networks:
      - vetnet

  gateway:
    build:
      context: .
      dockerfile: gateway/Dockerfile
    restart: unless-stopped
    ports:
      - "4050:4050"
    env_file: .env
    environment:
      - PORT=4050
      - NODE_ENV=development
      - MYSQL_HOST=${RDS_HOST}
      - MYSQL_POOL_MAX=20
      - TRACING_ENABLED=true
      - TRACING_EXPORTER_URL=http://tempo:9411/api/v2/spans
      - SERVICE_DISCOVERY_MODE=hybrid
      - SERVICE_AUTH=http://auth:4051
      - SERVICE_PATIENTS=http://patients:4052
      - SERVICE_MEDICAL=http://medical:4053
      - SERVICE_LAB=http://lab-imaging:4054
      - SERVICE_BILLING=http://billing:4055
      - SERVICE_TELEMEDICINE=http://telemedicine:4056
      - SERVICE_GROOMING=http://grooming:4057
      - SERVICE_REPORTS=http://reports:4058
      - SERVICE_NOTIFICATIONS=http://notifications:4059
      - SERVICE_PORTAL=http://portal:3010
      - SERVICE_AI=http://ai:4061
      - SERVICE_DOCUMENTS=http://documents:4062
      - REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4050/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 1024M
    networks:
      - vetnet

  auth:
    build:
      context: .
      dockerfile: services/auth/Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 4051
      NODE_ENV: development
      MYSQL_HOST: ${RDS_HOST}
      MYSQL_POOL_MAX: 20
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      TRACING_ENABLED: "true"
      TRACING_EXPORTER_URL: http://tempo:9411/api/v2/spans
      SERVICE_DISCOVERY_MODE: hybrid
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4051/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 768M
    networks:
      - vetnet

  patients:
    build:
      context: .
      dockerfile: services/patients/Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 4052
      NODE_ENV: development
      MYSQL_HOST: ${RDS_HOST}
      MYSQL_POOL_MAX: 20
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      TRACING_ENABLED: "true"
      TRACING_EXPORTER_URL: http://tempo:9411/api/v2/spans
      SERVICE_DISCOVERY_MODE: hybrid
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4052/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 768M
    networks:
      - vetnet

  medical:
    build:
      context: .
      dockerfile: services/medical/Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 4053
      NODE_ENV: development
      MYSQL_HOST: ${RDS_HOST}
      MYSQL_POOL_MAX: 20
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      TRACING_ENABLED: "true"
      TRACING_EXPORTER_URL: http://tempo:9411/api/v2/spans
      SERVICE_DISCOVERY_MODE: hybrid
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4053/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 768M
    networks:
      - vetnet

  lab-imaging:
    build:
      context: .
      dockerfile: services/lab-imaging/Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 4054
      NODE_ENV: development
      MYSQL_HOST: ${RDS_HOST}
      MYSQL_POOL_MAX: 20
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      TRACING_ENABLED: "true"
      TRACING_EXPORTER_URL: http://tempo:9411/api/v2/spans
      SERVICE_DISCOVERY_MODE: hybrid
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4054/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 768M
    networks:
      - vetnet

  billing:
    build:
      context: .
      dockerfile: services/billing/Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 4055
      NODE_ENV: development
      MYSQL_HOST: ${RDS_HOST}
      MYSQL_POOL_MAX: 20
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      TRACING_ENABLED: "true"
      TRACING_EXPORTER_URL: http://tempo:9411/api/v2/spans
      SERVICE_DISCOVERY_MODE: hybrid
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4055/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 768M
    networks:
      - vetnet

  telemedicine:
    build:
      context: .
      dockerfile: services/telemedicine/Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 4056
      NODE_ENV: development
      MYSQL_HOST: ${RDS_HOST}
      MYSQL_POOL_MAX: 20
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      TRACING_ENABLED: "true"
      TRACING_EXPORTER_URL: http://tempo:9411/api/v2/spans
      SERVICE_DISCOVERY_MODE: hybrid
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4056/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 768M
    networks:
      - vetnet

  grooming:
    build:
      context: .
      dockerfile: services/grooming/Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 4057
      NODE_ENV: development
      MYSQL_HOST: ${RDS_HOST}
      MYSQL_POOL_MAX: 20
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      TRACING_ENABLED: "true"
      TRACING_EXPORTER_URL: http://tempo:9411/api/v2/spans
      SERVICE_DISCOVERY_MODE: hybrid
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4057/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 768M
    networks:
      - vetnet

  reports:
    build:
      context: .
      dockerfile: services/reports/Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 4058
      NODE_ENV: development
      MYSQL_HOST: ${RDS_HOST}
      MYSQL_POOL_MAX: 20
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      TRACING_ENABLED: "true"
      TRACING_EXPORTER_URL: http://tempo:9411/api/v2/spans
      SERVICE_DISCOVERY_MODE: hybrid
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4058/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 768M
    networks:
      - vetnet

  notifications:
    build:
      context: .
      dockerfile: services/notifications/Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 4059
      NODE_ENV: development
      MYSQL_HOST: ${RDS_HOST}
      MYSQL_POOL_MAX: 20
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      TRACING_ENABLED: "true"
      TRACING_EXPORTER_URL: http://tempo:9411/api/v2/spans
      SERVICE_DISCOVERY_MODE: hybrid
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4059/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 768M
    networks:
      - vetnet

  portal:
    build:
      context: .
      dockerfile: services/portal/Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 3010
      NODE_ENV: development
      MYSQL_HOST: ${RDS_HOST}
      MYSQL_POOL_MAX: 20
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      TRACING_ENABLED: "true"
      TRACING_EXPORTER_URL: http://tempo:9411/api/v2/spans
      SERVICE_DISCOVERY_MODE: hybrid
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3010/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 768M
    networks:
      - vetnet

  ai:
    build:
      context: .
      dockerfile: services/ai/Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 4061
      NODE_ENV: development
      MYSQL_HOST: ${RDS_HOST}
      MYSQL_POOL_MAX: 20
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      TRACING_ENABLED: "true"
      TRACING_EXPORTER_URL: http://tempo:9411/api/v2/spans
      SERVICE_DISCOVERY_MODE: hybrid
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4061/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 1024M
    networks:
      - vetnet

  documents:
    build:
      context: .
      dockerfile: services/documents/Dockerfile
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 4062
      NODE_ENV: development
      MYSQL_HOST: ${RDS_HOST}
      MYSQL_POOL_MAX: 20
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      TRACING_ENABLED: "true"
      TRACING_EXPORTER_URL: http://tempo:9411/api/v2/spans
      SERVICE_DISCOVERY_MODE: hybrid
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4062/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 768M
    networks:
      - vetnet

  andromeda-front:
    build:
      context: ./andromeda-front
      dockerfile: Dockerfile
      args:
        VITE_API_URL: http://localhost:4050/api/v1
        VITE_PORTAL_ORG_ID: "1"
    restart: unless-stopped
    ports:
      - "5174:8080"
    networks:
      - vetnet
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 128M

  minio:
    image: minio/minio:RELEASE.2024-07-16T23-46-41Z
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY}
    # Ports NOT exposed to host — access via gateway presigned URLs only
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://127.0.0.1:9000/minio/health/live"]
      interval: 30s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1024M
    networks:
      - vetnet

  prometheus:
    image: prom/prometheus:v2.53.0
    restart: unless-stopped
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--web.enable-remote-write-receiver'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
      - '--web.enable-lifecycle'
    # Port NOT exposed to host — scrape via internal network only
    volumes:
      - ./docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./docker/prometheus/alerts.yml:/etc/prometheus/alerts.yml:ro
      - ./docker/prometheus/synthetics.yml:/etc/prometheus/synthetics.yml:ro
      - prometheus_data:/prometheus
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1024M
    networks:
      - vetnet

  alertmanager:
    image: prom/alertmanager:v0.27.0
    restart: unless-stopped
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
    volumes:
      - ./docker/alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
    networks:
      - vetnet

  blackbox-exporter:
    image: prom/blackbox-exporter:v0.25.0
    restart: unless-stopped
    command:
      - '--config.file=/etc/blackbox/blackbox.yml'
    volumes:
      - ./docker/blackbox/blackbox.yml:/etc/blackbox/blackbox.yml:ro
    networks:
      - vetnet

  tempo:
    image: grafana/tempo:2.5.0
    restart: unless-stopped
    command:
      - -config.file=/etc/tempo/tempo.yml
    volumes:
      - ./docker/tempo/tempo.yml:/etc/tempo/tempo.yml:ro
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 768M
    networks:
      - vetnet

  grafana:
    image: grafana/grafana:11.1.0
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:?GRAFANA_PASSWORD must be set in .env}
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_INSTALL_PLUGINS: grafana-clock-panel
    # Port NOT exposed to host — access via SSH tunnel or VPN in production
    volumes:
      - grafana_data:/var/lib/grafana
      - ./docker/grafana/datasources:/etc/grafana/provisioning/datasources:ro
      - ./docker/grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
    depends_on:
      - prometheus
      - tempo
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 768M
    networks:
      - vetnet

volumes:
  redis_data:
  minio_data:
  prometheus_data:
  grafana_data:

networks:
  vetnet:
    driver: bridge
